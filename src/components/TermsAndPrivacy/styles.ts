import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  termsContainer: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 24,
    marginBottom: 32,
  },
  roundCheckIcon: {
    width: 24,
    height: 24,
    marginRight: 16,
  },
});

export default styles;
